# Changelog

本项目的 notable 变更记录。版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [1.1.1] - 2026-06-12

### 新增

- **全局快捷键**：默认 `Alt + \`` 可显示/隐藏主窗口（托盘驻留时亦可用，便于游戏中快速唤出）。
- **快捷键配置**：配置面板新增「快捷键」标签，支持点击录制自定义组合键、恢复默认值；设置持久化至 `localStorage`。
- **注册失败提示**：启动或更换快捷键时，若组合键被其他程序占用或无效，会通过 toast 提示。

### 变更

- 主窗口 toggle 逻辑：可见且未最小化时隐藏至托盘；隐藏或最小化时恢复并聚焦。
- 版本号更新为 1.1.1（含 Cargo 同步）。

### 技术

- 接入 `tauri-plugin-global-shortcut` / `@tauri-apps/plugin-global-shortcut`。
- 新增 `src/lib/windowToggleShortcut.ts`、`src/components/ShortcutSettings.tsx`。
- 补充 Tauri 权限：`core:window:allow-hide`、`allow-is-visible`、`allow-is-minimized`、`allow-unminimize` 及 `global-shortcut:*`。

---

## [1.1.0] - 2026-06-11

### 新增

- **外观设置**：配置面板新增「外观」标签，支持本地持久化（`localStorage`，与保险库数据分离）。
- **预设主题**：极光玻璃、午夜纯色、深海、余烬、薄雾灰 5 套一键切换。
- **面板样式调节**：面板透明度、模糊强度、圆角滑块实时预览。
- **强调色**：在预设主题下方自定义按钮与高亮色；可一键恢复「跟随预设」。
- **背景定制**：
  - 背景类型：预设渐变 / 纯色着色 / 极简；
  - 背景着色（颜色 + 透明度）；
  - 背景图片（桌面版导入后自动缩放至最长边 1600px，保存至应用数据目录；超大文件仅提示不拦截）。
- **滚动条样式**：细窄玻璃风格滚动条，悬停/拖动时跟随当前强调色。

### 变更

- 界面色彩体系收拢为 CSS 变量（`--accent-*`、`--glass-*`、`--body-background` 等），预设与自定义外观统一由 `applyAppearance()` 应用。
- 启动时提前恢复主题（`index.html` 内联脚本 + `main.tsx`），减少主题闪烁。
- 版本号更新为 1.1.0（含 Cargo 同步）。

### 技术

- 新增 `src/lib/theme.ts`、`src/lib/appearanceAssets.ts`、`src/components/AppearanceSettings.tsx`。
- Tauri 启用 `assetProtocol`（`$APPDATA/**`），并补充应用数据目录读写相关权限，以支持背景图持久化与加载。

---

## [1.0.0] - 2026-06-10

### 新增

- **HuDaGee 账号管家**首个正式版本：基于 Tauri 2 + SolidJS + Vite 的 Windows 桌面应用。
- **本地加密保险库**：主密码创建/解锁；数据保存在应用数据目录，不上传云端。
- **分组与账号管理**：
  - 按游戏分组管理账号（角色名、账号、密码、职业、备注）；
  - 分组内搜索；可选显示/隐藏账号、密码列；
  - 单元格点击复制；账号表单新增/编辑/删除。
- **剪贴板快捷操作**：识别分享格式文本，一键填充新建账号；支持复制分享文本。
- **备份与恢复**：
  - 导出/导入 AES 加密备份（独立备份密码）；
  - 合并或替换现有保险库；
  - 保险库损坏时从备份恢复或重新创建。
- **配置中心**：分组重命名与拖拽排序、职业选项维护、主密码重置。
- **桌面集成**：
  - 系统托盘驻留，关闭窗口隐藏至托盘；
  - 单实例运行，重复启动唤起已有窗口；
  - 启动时延迟显示主窗口，避免白屏闪烁。
- **界面**：深色玻璃拟态风格，透明窗口；Windows NSIS 简体中文安装包。
